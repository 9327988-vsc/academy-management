# 학원 관리 시스템 MVP 개발 PRD

**버전:** MVP 1.0  
**작성일:** 2026년 3월 26일  
**개발 기간:** 6주  
**목표:** 핵심 기능만으로 실제 사용 가능한 최소 제품

---

## 🎯 MVP 범위 정의

### ✅ 포함되는 기능 (Must Have)
1. 회원가입/로그인 (선생님만)
2. 수업 생성 및 관리
3. 학생 추가
4. 출석 체크
5. 수업 종료 후 SMS 알림 발송
6. 간단한 대시보드

### ❌ 제외되는 기능 (Not Now)
- 보강 관리 시스템
- AI 문제 풀이
- 학생/학부모 로그인
- 카카오 알림톡
- 캘린더 뷰
- 통계/분석
- 학원 관리자 기능
- 파일 첨부

**MVP 철학:** "선생님이 수업 후 학생/학부모에게 알림을 쉽게 보낼 수 있다"

---

## 🏗️ 시스템 아키텍처

### 기술 스택

**Frontend**
```
React 18.3.1
├─ Vite 5.0 (빌드 도구)
├─ React Router 6 (라우팅)
├─ Tailwind CSS 3.4 (스타일링)
├─ Axios (HTTP 클라이언트)
└─ React Hook Form (폼 관리)
```

**Backend**
```
Node.js 20 LTS
├─ Express.js 4.19
├─ Prisma 5.x (ORM)
├─ MySQL 8.0
├─ JWT (인증)
├─ bcrypt (암호화)
└─ node-cron (스케줄러)
```

**External API**
```
- NHN Cloud SMS API (문자 발송)
```

---

## 📊 데이터베이스 설계 (MySQL)

### ERD 개요
```
User (선생님)
  ↓ 1:N
Class (수업)
  ↓ M:N (through ClassStudent)
Student (학생)
  ↓ 1:N
Parent (학부모)

ClassSession (수업 세션) ← 출석/알림의 기준
  ↓ 1:N
Attendance (출석 기록)

Notification (알림 발송 로그)
```

### 상세 스키마

```sql
-- 1. 사용자 (선생님만)
CREATE TABLE users (
    id VARCHAR(36) PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    role ENUM('teacher') DEFAULT 'teacher',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_email (email)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 2. 수업
CREATE TABLE classes (
    id VARCHAR(36) PRIMARY KEY,
    teacher_id VARCHAR(36) NOT NULL,
    name VARCHAR(100) NOT NULL,
    subject VARCHAR(50) NOT NULL,
    day_of_week VARCHAR(20) NOT NULL, -- '월,수,금' 형태
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    room VARCHAR(50),
    max_students INT DEFAULT 15,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (teacher_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_teacher (teacher_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 3. 학생
CREATE TABLE students (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20),
    grade VARCHAR(20),
    school VARCHAR(100),
    teacher_id VARCHAR(36) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (teacher_id) REFERENCES users(id) ON DELETE CASCADE,
    INDEX idx_teacher (teacher_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 4. 학부모
CREATE TABLE parents (
    id VARCHAR(36) PRIMARY KEY,
    student_id VARCHAR(36) NOT NULL,
    name VARCHAR(50) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    relationship VARCHAR(20), -- '부', '모', '조부모' 등
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    INDEX idx_student (student_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 5. 수업-학생 연결 (수강 정보)
CREATE TABLE class_students (
    id VARCHAR(36) PRIMARY KEY,
    class_id VARCHAR(36) NOT NULL,
    student_id VARCHAR(36) NOT NULL,
    enrolled_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (class_id) REFERENCES classes(id) ON DELETE CASCADE,
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    UNIQUE KEY unique_enrollment (class_id, student_id),
    INDEX idx_class (class_id),
    INDEX idx_student (student_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 6. 수업 세션 (실제 진행된 수업 기록)
CREATE TABLE class_sessions (
    id VARCHAR(36) PRIMARY KEY,
    class_id VARCHAR(36) NOT NULL,
    session_date DATE NOT NULL,
    start_time TIME NOT NULL,
    end_time TIME NOT NULL,
    topic VARCHAR(200),
    textbook VARCHAR(100),
    pages VARCHAR(50),
    key_concepts TEXT,
    homework TEXT,
    homework_due_date DATE,
    next_topic VARCHAR(200),
    special_notes TEXT,
    notification_sent BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (class_id) REFERENCES classes(id) ON DELETE CASCADE,
    INDEX idx_class_date (class_id, session_date),
    INDEX idx_notification_sent (notification_sent)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 7. 출석 기록
CREATE TABLE attendance (
    id VARCHAR(36) PRIMARY KEY,
    session_id VARCHAR(36) NOT NULL,
    student_id VARCHAR(36) NOT NULL,
    status ENUM('present', 'absent', 'late') NOT NULL,
    check_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    notes TEXT,
    FOREIGN KEY (session_id) REFERENCES class_sessions(id) ON DELETE CASCADE,
    FOREIGN KEY (student_id) REFERENCES students(id) ON DELETE CASCADE,
    UNIQUE KEY unique_attendance (session_id, student_id),
    INDEX idx_session (session_id),
    INDEX idx_student (student_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 8. 알림 발송 로그
CREATE TABLE notifications (
    id VARCHAR(36) PRIMARY KEY,
    session_id VARCHAR(36) NOT NULL,
    recipient_phone VARCHAR(20) NOT NULL,
    recipient_name VARCHAR(50) NOT NULL,
    recipient_type ENUM('student', 'parent') NOT NULL,
    content TEXT NOT NULL,
    status ENUM('pending', 'sent', 'failed') DEFAULT 'pending',
    sent_at TIMESTAMP,
    error_message TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (session_id) REFERENCES class_sessions(id) ON DELETE CASCADE,
    INDEX idx_session (session_id),
    INDEX idx_status (status),
    INDEX idx_sent_at (sent_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### Prisma Schema

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String
  phone     String
  role      Role     @default(teacher)
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  classes  Class[]
  students Student[]

  @@index([email])
  @@map("users")
}

enum Role {
  teacher
}

model Class {
  id          String   @id @default(uuid())
  teacherId   String   @map("teacher_id")
  name        String
  subject     String
  dayOfWeek   String   @map("day_of_week")
  startTime   String   @map("start_time")
  endTime     String   @map("end_time")
  room        String?
  maxStudents Int      @default(15) @map("max_students")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  teacher  User           @relation(fields: [teacherId], references: [id], onDelete: Cascade)
  students ClassStudent[]
  sessions ClassSession[]

  @@index([teacherId])
  @@map("classes")
}

model Student {
  id        String   @id @default(uuid())
  name      String
  phone     String?
  grade     String?
  school    String?
  teacherId String   @map("teacher_id")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  teacher     User           @relation(fields: [teacherId], references: [id], onDelete: Cascade)
  parents     Parent[]
  classes     ClassStudent[]
  attendance  Attendance[]

  @@index([teacherId])
  @@map("students")
}

model Parent {
  id           String   @id @default(uuid())
  studentId    String   @map("student_id")
  name         String
  phone        String
  relationship String?
  createdAt    DateTime @default(now()) @map("created_at")

  student Student @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@index([studentId])
  @@map("parents")
}

model ClassStudent {
  id         String   @id @default(uuid())
  classId    String   @map("class_id")
  studentId  String   @map("student_id")
  enrolledAt DateTime @default(now()) @map("enrolled_at")

  class   Class   @relation(fields: [classId], references: [id], onDelete: Cascade)
  student Student @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@unique([classId, studentId], name: "unique_enrollment")
  @@index([classId])
  @@index([studentId])
  @@map("class_students")
}

model ClassSession {
  id               String    @id @default(uuid())
  classId          String    @map("class_id")
  sessionDate      DateTime  @map("session_date") @db.Date
  startTime        String    @map("start_time")
  endTime          String    @map("end_time")
  topic            String?
  textbook         String?
  pages            String?
  keyConcepts      String?   @map("key_concepts") @db.Text
  homework         String?   @db.Text
  homeworkDueDate  DateTime? @map("homework_due_date") @db.Date
  nextTopic        String?   @map("next_topic")
  specialNotes     String?   @map("special_notes") @db.Text
  notificationSent Boolean   @default(false) @map("notification_sent")
  createdAt        DateTime  @default(now()) @map("created_at")

  class         Class          @relation(fields: [classId], references: [id], onDelete: Cascade)
  attendance    Attendance[]
  notifications Notification[]

  @@index([classId, sessionDate])
  @@index([notificationSent])
  @@map("class_sessions")
}

model Attendance {
  id        String           @id @default(uuid())
  sessionId String           @map("session_id")
  studentId String           @map("student_id")
  status    AttendanceStatus
  checkTime DateTime         @default(now()) @map("check_time")
  notes     String?          @db.Text

  session ClassSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  student Student      @relation(fields: [studentId], references: [id], onDelete: Cascade)

  @@unique([sessionId, studentId], name: "unique_attendance")
  @@index([sessionId])
  @@index([studentId])
  @@index([status])
  @@map("attendance")
}

enum AttendanceStatus {
  present
  absent
  late
}

model Notification {
  id            String             @id @default(uuid())
  sessionId     String             @map("session_id")
  recipientPhone String            @map("recipient_phone")
  recipientName String             @map("recipient_name")
  recipientType RecipientType      @map("recipient_type")
  content       String             @db.Text
  status        NotificationStatus @default(pending)
  sentAt        DateTime?          @map("sent_at")
  errorMessage  String?            @map("error_message") @db.Text
  createdAt     DateTime           @default(now()) @map("created_at")

  session ClassSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId])
  @@index([status])
  @@index([sentAt])
  @@map("notifications")
}

enum RecipientType {
  student
  parent
}

enum NotificationStatus {
  pending
  sent
  failed
}
```

---

## 🎨 화면 설계

### 1. 로그인 (`/login`)

```
┌───────────────────────────────────────┐
│                                       │
│           📚 학원 관리 시스템          │
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ 이메일                           │ │
│  │ [                        ]       │ │
│  └─────────────────────────────────┘ │
│                                       │
│  ┌─────────────────────────────────┐ │
│  │ 비밀번호                         │ │
│  │ [                        ]       │ │
│  └─────────────────────────────────┘ │
│                                       │
│  [ 로그인 ]                           │
│                                       │
│  계정이 없으신가요? 회원가입           │
│                                       │
└───────────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/auth/login
Request: {email, password}
Response: {accessToken, refreshToken, user: {id, name, email, role}}
```

---

### 2. 회원가입 (`/signup`)

```
┌───────────────────────────────────────┐
│                                       │
│           선생님 회원가입              │
│                                       │
│  이름                                 │
│  [                            ]       │
│                                       │
│  이메일                               │
│  [                            ]       │
│                                       │
│  전화번호                             │
│  [                            ]       │
│                                       │
│  비밀번호                             │
│  [                            ]       │
│  (최소 8자, 영문+숫자 포함)            │
│                                       │
│  비밀번호 확인                         │
│  [                            ]       │
│                                       │
│  [ 회원가입 ]                         │
│                                       │
│  이미 계정이 있으신가요? 로그인         │
│                                       │
└───────────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/auth/register
Request: {email, password, name, phone}
Response: {success: true, message: "회원가입 완료"}
```

---

### 3. 대시보드 (`/dashboard`)

```
┌──────────────────────────────────────────────────┐
│  📚 학원 관리 시스템      [🔔] [👤 김선생님 ▼]    │
├──────────────────────────────────────────────────┤
│  안녕하세요, 김선생님! 😊                         │
│  오늘 수업: 2개 | 전체 학생: 25명                 │
├──────────────────────────────────────────────────┤
│                                                  │
│  📅 오늘의 수업 (2026.03.26 화요일)               │
│  ┌────────────────────────────────────────────┐ │
│  │ 17:00 - 19:00                              │ │
│  │ 수학 중3 심화반                             │ │
│  │ 학생: 12명 | 교실: 301호                   │ │
│  │ [ 출석 체크 ] [ 수업 상세 ]                │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 19:00 - 21:00                              │ │
│  │ 영어 고1 기본반                             │ │
│  │ 학생: 8명 | 교실: 205호                    │ │
│  │ [ 출석 체크 ] [ 수업 상세 ]                │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  [ + 새 수업 만들기 ] [ 전체 수업 보기 ]          │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
GET /api/classes?date=2026-03-26
Response: {
  classes: [
    {id, name, subject, dayOfWeek, startTime, endTime, 
     studentCount, room}
  ]
}

GET /api/dashboard/stats
Response: {
  todayClasses: 2,
  totalStudents: 25
}
```

---

### 4. 수업 목록 (`/classes`)

```
┌──────────────────────────────────────────────────┐
│  📚 학원 관리 시스템      [🔔] [👤 김선생님 ▼]    │
├──────────────────────────────────────────────────┤
│  📖 내 수업 관리                                  │
│  [ + 새 수업 만들기 ]                            │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 수학 중3 심화반                             │ │
│  │ 과목: 수학 | 교실: 301호                   │ │
│  │ 일정: 월, 수, 금 17:00-19:00              │ │
│  │ 학생: 12/15명                              │ │
│  │                                            │ │
│  │ [ 출석부 ] [ 학생 관리 ] [ 수정 ] [ 삭제 ] │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 영어 고1 기본반                             │ │
│  │ 과목: 영어 | 교실: 205호                   │ │
│  │ 일정: 화, 목 19:00-21:00                  │ │
│  │ 학생: 8/12명                               │ │
│  │                                            │ │
│  │ [ 출석부 ] [ 학생 관리 ] [ 수정 ] [ 삭제 ] │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
GET /api/classes
Response: {
  classes: [
    {id, name, subject, dayOfWeek, startTime, endTime, 
     room, maxStudents, studentCount}
  ]
}

DELETE /api/classes/:id
Response: {success: true}
```

---

### 5. 수업 생성 모달

```
┌───────────────────────────────────┐
│  새 수업 만들기               [X] │
├───────────────────────────────────┤
│                                   │
│  수업명 *                         │
│  [수학 중3 심화반          ]      │
│                                   │
│  과목 *                           │
│  [수학 ▼]                         │
│  - 수학, 영어, 과학, 국어, 사회... │
│                                   │
│  교실                             │
│  [301호                    ]      │
│                                   │
│  수업 요일 * (복수 선택)           │
│  [✓] 월  [ ] 화  [✓] 수          │
│  [ ] 목  [✓] 금  [ ] 토  [ ] 일  │
│                                   │
│  수업 시간 *                       │
│  [17:00 ▼] ~ [19:00 ▼]           │
│                                   │
│  정원                             │
│  [15                       ]명    │
│                                   │
│  [ 취소 ] [ 생성하기 ]            │
│                                   │
└───────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/classes
Request: {
  name: "수학 중3 심화반",
  subject: "수학",
  dayOfWeek: "월,수,금",
  startTime: "17:00",
  endTime: "19:00",
  room: "301호",
  maxStudents: 15
}
Response: {success: true, class: {...}}
```

---

### 6. 학생 관리 (`/classes/:id/students`)

```
┌──────────────────────────────────────────────────┐
│  📚 학원 관리 시스템      [🔔] [👤 김선생님 ▼]    │
├──────────────────────────────────────────────────┤
│  📖 수학 중3 심화반 - 학생 관리                   │
│  [ ← 뒤로 ] [ + 학생 추가 ]                      │
├──────────────────────────────────────────────────┤
│                                                  │
│  전체 학생: 12/15명                               │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 1. 김철수 (중3)                            │ │
│  │    전화: 010-1234-5678                     │ │
│  │    학부모: 김아무개 (부) 010-1111-2222     │ │
│  │    [ 수정 ] [ 삭제 ]                       │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 2. 이영희 (중3)                            │ │
│  │    전화: 010-2345-6789                     │ │
│  │    학부모: 이어머니 (모) 010-2222-3333     │ │
│  │    [ 수정 ] [ 삭제 ]                       │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ...                                             │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
GET /api/classes/:id/students
Response: {
  students: [
    {
      id, name, phone, grade, school,
      parents: [{id, name, phone, relationship}]
    }
  ]
}

DELETE /api/classes/:classId/students/:studentId
Response: {success: true}
```

---

### 7. 학생 추가 모달

```
┌───────────────────────────────────┐
│  학생 추가                    [X] │
├───────────────────────────────────┤
│                                   │
│  학생 정보                        │
│  ────────────────────────         │
│  이름 *                           │
│  [                         ]      │
│                                   │
│  학년                             │
│  [중3 ▼]                          │
│                                   │
│  학교                             │
│  [                         ]      │
│                                   │
│  전화번호                         │
│  [                         ]      │
│                                   │
│  학부모 정보                      │
│  ────────────────────────         │
│  이름 *                           │
│  [                         ]      │
│                                   │
│  관계 *                           │
│  [부 ▼] (부/모/조부모 등)         │
│                                   │
│  전화번호 *                       │
│  [                         ]      │
│                                   │
│  [ + 학부모 추가 ]                │
│                                   │
│  [ 취소 ] [ 추가하기 ]            │
│                                   │
└───────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/students
Request: {
  name: "김철수",
  grade: "중3",
  school: "○○중학교",
  phone: "010-1234-5678",
  parents: [
    {
      name: "김아무개",
      relationship: "부",
      phone: "010-1111-2222"
    }
  ]
}
Response: {success: true, student: {...}}

POST /api/classes/:id/enroll
Request: {studentId: "..."}
Response: {success: true}
```

---

### 8. 출석 체크 (`/classes/:id/attendance`)

```
┌──────────────────────────────────────────────────┐
│  📚 학원 관리 시스템      [🔔] [👤 김선생님 ▼]    │
├──────────────────────────────────────────────────┤
│  📖 수학 중3 심화반 - 출석부                      │
│  [ ← 뒤로 ]                                      │
├──────────────────────────────────────────────────┤
│                                                  │
│  날짜: [2026-03-26 ▼]                            │
│  출석: 0/12명                                     │
│                                                  │
│  [ 전체 출석 ] [ 전체 결석 ]                      │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 1. 김철수                                  │ │
│  │    [✓ 출석] [✗ 결석] [△ 지각]             │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 2. 이영희                                  │ │
│  │    [✓ 출석] [✗ 결석] [△ 지각]             │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ┌────────────────────────────────────────────┐ │
│  │ 3. 박민수                                  │ │
│  │    [✓ 출석] [✗ 결석] [△ 지각]             │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  ...                                             │
│                                                  │
│  [ 다음: 수업 종료 처리 → ]                      │
│                                                  │
└──────────────────────────────────────────────────┘
```

**상태 변화:**
- 버튼 클릭 시 색상 변경
  * 출석(✓): 초록색 배경
  * 결석(✗): 빨간색 배경
  * 지각(△): 노란색 배경
- 선택된 버튼만 활성화 표시

---

### 9. 수업 종료 처리 (`/classes/:id/complete`)

```
┌──────────────────────────────────────────────────┐
│  📚 학원 관리 시스템      [🔔] [👤 김선생님 ▼]    │
├──────────────────────────────────────────────────┤
│  📖 수업 종료 처리                                │
│  [ ← 뒤로 ]                                      │
├──────────────────────────────────────────────────┤
│                                                  │
│  수업: 수학 중3 심화반                            │
│  날짜: 2026.03.26 (화) 17:00-19:00               │
│  출석: 10명 / 결석: 2명                           │
│                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                  │
│  📚 오늘 진도                                     │
│  교재명                                          │
│  [최상위 수학                           ]        │
│                                                  │
│  페이지                                          │
│  [124    ] ~ [131    ]                           │
│                                                  │
│  핵심 개념                                       │
│  [이차방정식 활용 - 속력/거리 문제      ]        │
│  [                                      ]        │
│                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                  │
│  📅 다음 수업 예고                                │
│  날짜: [2026-03-28 ▼]                            │
│  예정 내용                                       │
│  [이차함수 그래프 (1단원 마무리)        ]        │
│                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                  │
│  📝 숙제                                          │
│  내용                                            │
│  [최상위 수학 p.132-135 (8문제)         ]        │
│  [                                      ]        │
│                                                  │
│  제출 기한: [2026-03-28 ▼]                       │
│                                                  │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                  │
│  💬 특이사항 (선택)                               │
│  [                                      ]        │
│  [                                      ]        │
│                                                  │
│  [ 취소 ] [ 알림 미리보기 ]                      │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/sessions
Request: {
  classId: "...",
  sessionDate: "2026-03-26",
  startTime: "17:00",
  endTime: "19:00",
  topic: "이차방정식 활용",
  textbook: "최상위 수학",
  pages: "p.124-131",
  keyConcepts: "속력/거리 문제 풀이",
  homework: "최상위 수학 p.132-135 (8문제)",
  homeworkDueDate: "2026-03-28",
  nextTopic: "이차함수 그래프",
  specialNotes: "..."
}
Response: {success: true, session: {...}}

POST /api/attendance/bulk
Request: {
  sessionId: "...",
  attendance: [
    {studentId: "S001", status: "present"},
    {studentId: "S002", status: "absent"}
  ]
}
Response: {success: true}
```

---

### 10. 알림 미리보기

```
┌──────────────────────────────────────────────────┐
│  📨 알림 미리보기                            [X]  │
├──────────────────────────────────────────────────┤
│                                                  │
│  📤 출석 학생 (10명)                              │
│  ┌────────────────────────────────────────────┐ │
│  │ [○○학원] 김철수 학생 수업 알림             │ │
│  │                                            │ │
│  │ 📚 오늘 수업 (3/26 화)                     │ │
│  │ - 과목: 수학 중3 심화반                    │ │
│  │ - 진도: 이차방정식 활용 (p.124-131)        │ │
│  │ - 핵심: 속력/거리 문제 풀이                │ │
│  │                                            │ │
│  │ 📝 숙제                                    │ │
│  │ - 최상위 수학 p.132-135 (8문제)           │ │
│  │ - 제출: 3/28(목) 수업 전                  │ │
│  │                                            │ │
│  │ 📅 다음 수업                               │ │
│  │ - 3/28(목) 17:00                          │ │
│  │ - 예정: 이차함수 그래프                    │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  📤 결석 학생 (2명)                               │
│  ┌────────────────────────────────────────────┐ │
│  │ [○○학원] 신동엽 학생 결석 알림             │ │
│  │                                            │ │
│  │ ⚠️ 오늘(3/26 화) 수업에 결석하셨습니다.    │ │
│  │                                            │ │
│  │ 📚 오늘 진도                               │ │
│  │ - 이차방정식 활용 (p.124-131)              │ │
│  │                                            │ │
│  │ 📝 과제                                    │ │
│  │ - 최상위 수학 p.132-135 (8문제)           │ │
│  │ - 제출: 3/28(목) 수업 전                  │ │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  발송 대상: 학생 10명 + 학부모 12명 = 총 22건     │
│  발송 방법: SMS 문자 메시지                       │
│                                                  │
│  [ 취소 ] [ 알림 발송하기 ]                      │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/sessions/:id/preview-notification
Response: {
  presentStudents: [
    {
      studentName: "김철수",
      message: "...",
      recipients: [
        {type: "student", name: "김철수", phone: "010-1234-5678"},
        {type: "parent", name: "김아무개", phone: "010-1111-2222"}
      ]
    }
  ],
  absentStudents: [...]
}
```

---

### 11. 알림 발송 결과

```
┌──────────────────────────────────────────────────┐
│  ✅ 알림 발송 완료                               │
├──────────────────────────────────────────────────┤
│                                                  │
│  📊 발송 결과                                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│  총 발송: 22건                                    │
│  ✓ 성공: 22건                                    │
│  ✗ 실패: 0건                                     │
│                                                  │
│  발송 내역                                       │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                                  │
│  ✓ 김철수 (학생) - 010-1234-5678                │
│  ✓ 김아무개 (학부모) - 010-1111-2222             │
│  ✓ 이영희 (학생) - 010-2345-6789                │
│  ✓ 이어머니 (학부모) - 010-2222-3333             │
│  ...                                             │
│                                                  │
│  발송 시간: 2026.03.26 19:15                     │
│                                                  │
│  [ 확인 ] [ 발송 내역 다운로드 ]                 │
│                                                  │
└──────────────────────────────────────────────────┘
```

**API 엔드포인트:**
```
POST /api/sessions/:id/send-notification
Response: {
  success: true,
  total: 22,
  sent: 22,
  failed: 0,
  details: [
    {
      recipientName: "김철수",
      recipientPhone: "010-1234-5678",
      recipientType: "student",
      status: "sent",
      sentAt: "2026-03-26T19:15:00Z"
    }
  ]
}

GET /api/notifications?sessionId=...
Response: {
  notifications: [...]
}
```

---

## 🔌 API 명세서 (전체)

### 인증 (Authentication)

```javascript
// 회원가입
POST /api/auth/register
Request: {
  email: string,
  password: string,
  name: string,
  phone: string
}
Response: {
  success: boolean,
  message: string
}

// 로그인
POST /api/auth/login
Request: {
  email: string,
  password: string
}
Response: {
  success: boolean,
  accessToken: string,
  refreshToken: string,
  user: {
    id: string,
    name: string,
    email: string,
    role: string
  }
}

// 토큰 갱신
POST /api/auth/refresh
Request: {
  refreshToken: string
}
Response: {
  accessToken: string
}

// 로그아웃
POST /api/auth/logout
Response: {
  success: boolean
}
```

---

### 사용자 (User)

```javascript
// 내 정보 조회
GET /api/users/me
Headers: Authorization: Bearer <token>
Response: {
  id: string,
  name: string,
  email: string,
  phone: string,
  role: string
}

// 내 정보 수정
PATCH /api/users/me
Request: {
  name?: string,
  phone?: string
}
Response: {
  success: boolean,
  user: {...}
}
```

---

### 수업 (Class)

```javascript
// 수업 목록 조회
GET /api/classes
Query: ?date=2026-03-26 (선택, 특정 날짜의 수업만)
Response: {
  classes: [
    {
      id: string,
      name: string,
      subject: string,
      dayOfWeek: string,
      startTime: string,
      endTime: string,
      room: string,
      maxStudents: number,
      studentCount: number,
      createdAt: string
    }
  ]
}

// 수업 상세 조회
GET /api/classes/:id
Response: {
  id: string,
  name: string,
  subject: string,
  dayOfWeek: string,
  startTime: string,
  endTime: string,
  room: string,
  maxStudents: number,
  teacher: {id, name, phone},
  students: [
    {
      id, name, phone, grade, school,
      parents: [{id, name, phone, relationship}]
    }
  ]
}

// 수업 생성
POST /api/classes
Request: {
  name: string,
  subject: string,
  dayOfWeek: string, // "월,수,금"
  startTime: string, // "17:00"
  endTime: string,   // "19:00"
  room?: string,
  maxStudents?: number
}
Response: {
  success: boolean,
  class: {...}
}

// 수업 수정
PATCH /api/classes/:id
Request: {
  name?: string,
  subject?: string,
  dayOfWeek?: string,
  startTime?: string,
  endTime?: string,
  room?: string,
  maxStudents?: number
}
Response: {
  success: boolean,
  class: {...}
}

// 수업 삭제
DELETE /api/classes/:id
Response: {
  success: boolean
}
```

---

### 학생 (Student)

```javascript
// 학생 생성
POST /api/students
Request: {
  name: string,
  phone?: string,
  grade?: string,
  school?: string,
  parents: [
    {
      name: string,
      phone: string,
      relationship: string
    }
  ]
}
Response: {
  success: boolean,
  student: {id, name, ...}
}

// 학생 수정
PATCH /api/students/:id
Request: {
  name?: string,
  phone?: string,
  grade?: string,
  school?: string
}
Response: {
  success: boolean,
  student: {...}
}

// 학생 삭제
DELETE /api/students/:id
Response: {
  success: boolean
}

// 학부모 추가
POST /api/students/:id/parents
Request: {
  name: string,
  phone: string,
  relationship: string
}
Response: {
  success: boolean,
  parent: {...}
}

// 학부모 삭제
DELETE /api/parents/:id
Response: {
  success: boolean
}
```

---

### 수업-학생 연결

```javascript
// 수업에 학생 추가
POST /api/classes/:id/enroll
Request: {
  studentId: string
}
Response: {
  success: boolean
}

// 수업에서 학생 제거
DELETE /api/classes/:classId/students/:studentId
Response: {
  success: boolean
}

// 수업의 학생 목록 조회
GET /api/classes/:id/students
Response: {
  students: [
    {
      id, name, phone, grade, school,
      parents: [{id, name, phone, relationship}]
    }
  ]
}
```

---

### 수업 세션 (ClassSession)

```javascript
// 수업 세션 생성 (수업 종료 처리)
POST /api/sessions
Request: {
  classId: string,
  sessionDate: string, // "2026-03-26"
  startTime: string,
  endTime: string,
  topic?: string,
  textbook?: string,
  pages?: string,
  keyConcepts?: string,
  homework?: string,
  homeworkDueDate?: string,
  nextTopic?: string,
  specialNotes?: string
}
Response: {
  success: boolean,
  session: {id, ...}
}

// 세션 상세 조회
GET /api/sessions/:id
Response: {
  id, classId, sessionDate, startTime, endTime,
  topic, textbook, pages, keyConcepts, homework,
  homeworkDueDate, nextTopic, specialNotes,
  notificationSent,
  class: {id, name, subject},
  attendance: [
    {studentId, studentName, status}
  ]
}

// 수업의 세션 목록 조회
GET /api/classes/:id/sessions
Query: ?limit=10&offset=0
Response: {
  sessions: [{id, sessionDate, topic, notificationSent}],
  total: number
}
```

---

### 출석 (Attendance)

```javascript
// 출석 일괄 등록
POST /api/attendance/bulk
Request: {
  sessionId: string,
  attendance: [
    {
      studentId: string,
      status: "present" | "absent" | "late",
      notes?: string
    }
  ]
}
Response: {
  success: boolean,
  count: number
}

// 개별 출석 수정
PATCH /api/attendance/:id
Request: {
  status: "present" | "absent" | "late",
  notes?: string
}
Response: {
  success: boolean,
  attendance: {...}
}

// 세션의 출석 현황 조회
GET /api/sessions/:id/attendance
Response: {
  sessionId: string,
  sessionDate: string,
  attendance: [
    {
      id, studentId, studentName, status, 
      checkTime, notes
    }
  ],
  stats: {
    present: number,
    absent: number,
    late: number,
    total: number
  }
}
```

---

### 알림 (Notification)

```javascript
// 알림 미리보기
POST /api/sessions/:id/preview-notification
Response: {
  presentStudents: [
    {
      studentId: string,
      studentName: string,
      message: string,
      recipients: [
        {
          type: "student" | "parent",
          name: string,
          phone: string
        }
      ]
    }
  ],
  absentStudents: [...],
  totalRecipients: number
}

// 알림 발송
POST /api/sessions/:id/send-notification
Response: {
  success: boolean,
  total: number,
  sent: number,
  failed: number,
  details: [
    {
      recipientName: string,
      recipientPhone: string,
      recipientType: "student" | "parent",
      status: "sent" | "failed",
      sentAt?: string,
      errorMessage?: string
    }
  ]
}

// 알림 발송 내역 조회
GET /api/notifications
Query: ?sessionId=...&status=...&limit=20&offset=0
Response: {
  notifications: [
    {
      id, sessionId, recipientPhone, recipientName,
      recipientType, content, status, sentAt,
      errorMessage
    }
  ],
  total: number
}
```

---

### 대시보드 통계

```javascript
// 대시보드 통계
GET /api/dashboard/stats
Response: {
  todayClasses: number,
  totalStudents: number,
  totalClasses: number,
  recentSessions: [
    {id, classId, className, sessionDate, notificationSent}
  ]
}
```

---

## 🔐 인증 미들웨어

### JWT 토큰 검증

```javascript
// middleware/authMiddleware.js

const jwt = require('jsonwebtoken');

const verifyToken = (req, res, next) => {
  const authHeader = req.headers.authorization;
  
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return res.status(401).json({
      success: false,
      message: '인증 토큰이 필요합니다.'
    });
  }

  const token = authHeader.substring(7);

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded; // {userId, email, role}
    next();
  } catch (error) {
    return res.status(401).json({
      success: false,
      message: '유효하지 않은 토큰입니다.'
    });
  }
};

module.exports = { verifyToken };
```

**사용 방법:**
```javascript
const { verifyToken } = require('./middleware/authMiddleware');

// 보호된 라우트
router.get('/api/classes', verifyToken, getClasses);
router.post('/api/classes', verifyToken, createClass);
```

---

## 📱 SMS 발송 서비스

### NHN Cloud SMS API 연동

```javascript
// services/smsService.js

const axios = require('axios');

class SMSService {
  constructor() {
    this.appKey = process.env.NHN_APP_KEY;
    this.secretKey = process.env.NHN_SECRET_KEY;
    this.sendNo = process.env.SMS_SENDER_NUMBER;
    this.apiUrl = 'https://api-sms.cloud.toast.com/sms/v3.0/appKeys';
  }

  async sendSMS(recipientNo, content) {
    try {
      const response = await axios.post(
        `${this.apiUrl}/${this.appKey}/sender/sms`,
        {
          body: content,
          sendNo: this.sendNo,
          recipientList: [
            {
              recipientNo: recipientNo,
              internationalRecipientNo: recipientNo
            }
          ]
        },
        {
          headers: {
            'Content-Type': 'application/json;charset=UTF-8',
            'X-Secret-Key': this.secretKey
          }
        }
      );

      return {
        success: response.data.header.isSuccessful,
        requestId: response.data.body.data.requestId
      };
    } catch (error) {
      console.error('SMS 발송 실패:', error);
      return {
        success: false,
        error: error.message
      };
    }
  }

  async sendBulkSMS(recipients) {
    // recipients: [{recipientNo, content}]
    const results = [];

    for (const recipient of recipients) {
      const result = await this.sendSMS(
        recipient.recipientNo,
        recipient.content
      );
      
      results.push({
        recipientNo: recipient.recipientNo,
        ...result
      });

      // API 호출 제한 고려 (초당 100건)
      await new Promise(resolve => setTimeout(resolve, 10));
    }

    return results;
  }

  // 알림 메시지 생성
  generateSessionNotification(sessionData, studentData, isAbsent = false) {
    const { className, sessionDate, topic, textbook, pages, 
            homework, homeworkDueDate, nextDate, nextTopic } = sessionData;
    
    if (isAbsent) {
      return `[○○학원] ${studentData.name} 학생 결석 알림

⚠️ 오늘(${sessionDate}) 수업에 결석하셨습니다.

📚 오늘 진도
- ${topic} (${textbook} ${pages})

📝 과제
- ${homework}
- 제출: ${homeworkDueDate}

문의: 선생님 연락처`;
    }

    return `[○○학원] ${studentData.name} 학생 수업 알림

📚 오늘 수업 (${sessionDate})
- 과목: ${className}
- 진도: ${topic} (${textbook} ${pages})

📝 숙제
- ${homework}
- 제출: ${homeworkDueDate}

📅 다음 수업
- ${nextDate}
- 예정: ${nextTopic}

문의: 선생님 연락처`;
  }
}

module.exports = new SMSService();
```

---

## 🎨 Frontend 구조

### 폴더 구조

```
frontend/
├── public/
├── src/
│   ├── components/
│   │   ├── common/
│   │   │   ├── Button.jsx
│   │   │   ├── Input.jsx
│   │   │   ├── Modal.jsx
│   │   │   └── Navbar.jsx
│   │   ├── classes/
│   │   │   ├── ClassCard.jsx
│   │   │   ├── ClassForm.jsx
│   │   │   └── ClassList.jsx
│   │   ├── students/
│   │   │   ├── StudentCard.jsx
│   │   │   ├── StudentForm.jsx
│   │   │   └── StudentList.jsx
│   │   └── attendance/
│   │       ├── AttendanceRow.jsx
│   │       └── AttendanceStats.jsx
│   ├── pages/
│   │   ├── auth/
│   │   │   ├── Login.jsx
│   │   │   └── Signup.jsx
│   │   ├── Dashboard.jsx
│   │   ├── Classes.jsx
│   │   ├── ClassDetail.jsx
│   │   ├── Students.jsx
│   │   ├── Attendance.jsx
│   │   └── SessionComplete.jsx
│   ├── api/
│   │   ├── axios.js (Axios 인스턴스)
│   │   ├── auth.js
│   │   ├── classes.js
│   │   ├── students.js
│   │   ├── sessions.js
│   │   └── notifications.js
│   ├── hooks/
│   │   ├── useAuth.js
│   │   ├── useClasses.js
│   │   └── useStudents.js
│   ├── utils/
│   │   ├── formatDate.js
│   │   ├── formatPhone.js
│   │   └── validation.js
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
├── package.json
├── vite.config.js
└── tailwind.config.js
```

### Axios 설정

```javascript
// api/axios.js

import axios from 'axios';

const axiosInstance = axios.create({
  baseURL: import.meta.env.VITE_API_URL || 'http://localhost:3000/api',
  timeout: 10000,
  headers: {
    'Content-Type': 'application/json'
  }
});

// 요청 인터셉터 (토큰 자동 추가)
axiosInstance.interceptors.request.use(
  (config) => {
    const token = localStorage.getItem('accessToken');
    if (token) {
      config.headers.Authorization = `Bearer ${token}`;
    }
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 응답 인터셉터 (에러 처리)
axiosInstance.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;

    // 401 에러이고 아직 재시도하지 않았다면
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;

      try {
        const refreshToken = localStorage.getItem('refreshToken');
        const response = await axios.post(
          `${axiosInstance.defaults.baseURL}/auth/refresh`,
          { refreshToken }
        );

        const { accessToken } = response.data;
        localStorage.setItem('accessToken', accessToken);

        originalRequest.headers.Authorization = `Bearer ${accessToken}`;
        return axiosInstance(originalRequest);
      } catch (refreshError) {
        // 리프레시 토큰도 만료됨 → 로그아웃
        localStorage.removeItem('accessToken');
        localStorage.removeItem('refreshToken');
        window.location.href = '/login';
        return Promise.reject(refreshError);
      }
    }

    return Promise.reject(error);
  }
);

export default axiosInstance;
```

---

## 🚀 개발 로드맵 (6주)

### Week 1: 프로젝트 초기 설정 + 인증

**목표:** 프로젝트 구조 생성 및 로그인/회원가입 완성

**작업 내역:**
1. Frontend/Backend 프로젝트 생성
2. Prisma 설정 및 DB 연결
3. User 테이블 마이그레이션
4. 회원가입 API 구현
5. 로그인 API + JWT 발급
6. 인증 미들웨어 구현
7. 로그인/회원가입 페이지 구현

**완료 기준:**
- ✅ 회원가입 후 DB에 사용자 저장 확인
- ✅ 로그인 시 JWT 토큰 발급 확인
- ✅ 보호된 라우트 접근 시 토큰 검증 확인

---

### Week 2: 수업 관리

**목표:** 수업 CRUD 완성

**작업 내역:**
1. Class 테이블 마이그레이션
2. 수업 생성 API
3. 수업 목록 조회 API
4. 수업 수정/삭제 API
5. 대시보드 페이지 구현
6. 수업 목록 페이지 구현
7. 수업 생성 모달 구현

**완료 기준:**
- ✅ 수업 생성 후 목록에 표시 확인
- ✅ 수업 수정/삭제 작동 확인
- ✅ 대시보드에 오늘 수업 표시 확인

---

### Week 3: 학생 관리

**목표:** 학생 추가 및 수업 연결

**작업 내역:**
1. Student, Parent, ClassStudent 테이블 마이그레이션
2. 학생 생성 API (학부모 포함)
3. 수업-학생 연결 API
4. 학생 목록 조회 API
5. 학생 관리 페이지 구현
6. 학생 추가 모달 구현 (학부모 포함)
7. 수업별 학생 목록 페이지 구현

**완료 기준:**
- ✅ 학생 생성 시 학부모도 함께 저장 확인
- ✅ 수업에 학생 추가 후 목록에 표시 확인
- ✅ 학생 삭제 시 연결 관계도 삭제 확인

---

### Week 4: 출석 체크

**목표:** 출석 체크 시스템 완성

**작업 내역:**
1. ClassSession, Attendance 테이블 마이그레이션
2. 출석 일괄 등록 API
3. 출석 현황 조회 API
4. 출석 체크 페이지 구현
5. 전체 출석/결석 버튼 구현
6. 출석 상태별 색상 표시

**완료 기준:**
- ✅ 출석 체크 후 DB 저장 확인
- ✅ 출석률 계산 정확성 확인
- ✅ 날짜별 출석 현황 조회 확인

---

### Week 5: 수업 종료 처리

**목표:** 수업 종료 후 세션 정보 입력

**작업 내역:**
1. 세션 생성 API
2. 수업 종료 처리 페이지 구현
3. 진도/숙제/다음 수업 입력 폼
4. 출석 정보와 세션 연결
5. 알림 미리보기 API
6. 알림 미리보기 페이지 구현

**완료 기준:**
- ✅ 수업 종료 처리 시 세션 생성 확인
- ✅ 출석 정보와 세션 연결 확인
- ✅ 알림 미리보기 정상 표시 확인

---

### Week 6: SMS 알림 발송

**목표:** SMS 알림 발송 완성

**작업 내역:**
1. Notification 테이블 마이그레이션
2. NHN Cloud SMS API 연동
3. 알림 메시지 생성 로직
4. 알림 발송 API
5. 알림 발송 결과 페이지 구현
6. 발송 내역 조회 페이지 구현
7. 에러 처리 및 재발송 기능

**완료 기준:**
- ✅ SMS 발송 성공 확인 (실제 문자 수신)
- ✅ 발송 실패 시 에러 로그 저장 확인
- ✅ 발송 내역 조회 정상 작동 확인

---

## ⚙️ 환경 설정

### Backend `.env` 파일

```env
# 서버 설정
PORT=3000
NODE_ENV=development

# 데이터베이스
DATABASE_URL="mysql://root:password@localhost:3306/academy_mvp"

# JWT
JWT_SECRET="your-very-long-secret-key-here-change-in-production"
JWT_REFRESH_SECRET="your-refresh-secret-key-here"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# NHN Cloud SMS
NHN_APP_KEY="your-nhn-app-key"
NHN_SECRET_KEY="your-nhn-secret-key"
SMS_SENDER_NUMBER="01012345678"

# CORS
CORS_ORIGIN="http://localhost:5173"
```

### Frontend `.env` 파일

```env
VITE_API_URL=http://localhost:3000/api
```

---

## 🧪 테스트 시나리오

### 기본 플로우 테스트

```
1. 회원가입
   → 이메일: teacher@test.com
   → 비밀번호: Test1234!
   → 이름: 김선생님
   → 전화: 010-1234-5678

2. 로그인
   → 토큰 발급 확인
   → 대시보드 접근 확인

3. 수업 생성
   → 수업명: 수학 중3 심화반
   → 과목: 수학
   → 요일: 월, 수, 금
   → 시간: 17:00-19:00

4. 학생 추가
   → 이름: 김철수
   → 학년: 중3
   → 전화: 010-1111-1111
   → 학부모: 김아무개 (부) 010-2222-2222

5. 수업에 학생 연결
   → 수학 중3 심화반 → 학생 추가 → 김철수 선택

6. 출석 체크
   → 날짜: 오늘
   → 김철수: 출석

7. 수업 종료 처리
   → 진도: 이차방정식 활용
   → 숙제: 문제집 p.50-55
   → 알림 미리보기 확인

8. SMS 발송
   → 발송 버튼 클릭
   → 발송 결과 확인
   → 실제 휴대폰 문자 수신 확인
```

---

## 📊 MVP 성공 지표

### 필수 검증 항목

1. **기능 완성도**
   - ✅ 선생님이 5분 안에 수업을 생성할 수 있다
   - ✅ 학생 10명 출석 체크가 1분 안에 완료된다
   - ✅ 수업 종료 처리가 2분 안에 완료된다
   - ✅ SMS 알림이 30초 내에 발송된다

2. **안정성**
   - ✅ 에러 없이 전체 플로우 완료 가능
   - ✅ 동시 10명 접속 시에도 정상 작동
   - ✅ 데이터 유실 없음

3. **사용성**
   - ✅ 튜토리얼 없이도 사용 가능한 직관적 UI
   - ✅ 모바일 화면에서도 사용 가능 (반응형)
   - ✅ 3회 이내 클릭으로 주요 작업 완료

---

## 🚫 MVP에서 제외된 기능 (추후 추가)

1. ❌ 보강 관리 시스템
2. ❌ AI 문제 풀이
3. ❌ 학생/학부모 로그인
4. ❌ 카카오 알림톡
5. ❌ 캘린더 뷰
6. ❌ 통계 및 분석 대시보드
7. ❌ 성적 관리
8. ❌ 상담 예약
9. ❌ 파일 첨부
10. ❌ 학원 관리자 기능

---

## 💡 개발 팁

### 1. Git 브랜치 전략
```
main (프로덕션)
└─ develop (개발)
   ├─ feature/auth (인증)
   ├─ feature/classes (수업 관리)
   ├─ feature/students (학생 관리)
   ├─ feature/attendance (출석)
   └─ feature/notifications (알림)
```

### 2. 커밋 메시지 컨벤션
```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 포맷팅
refactor: 리팩토링
test: 테스트 코드
chore: 빌드/설정 변경
```

### 3. 개발 우선순위
1. 백엔드 API 먼저 완성 (Postman으로 테스트)
2. 프론트엔드 연동
3. UI/UX 개선
4. 에러 처리 강화

---

## 📅 다음 단계 (MVP 이후)

### Phase 2: 기능 확장
- 보강 관리 시스템 추가
- 학생/학부모 로그인 구현
- 카카오 알림톡 연동
- 캘린더 뷰 추가

### Phase 3: AI 기능
- AI 문제 풀이 시스템
- 학습 분석 대시보드

---

**문서 버전:** MVP 1.0  
**최종 수정:** 2026년 3월 26일  
**다음 리뷰:** Week 3 완료 후 (중간 점검)
